// Should generate a token

// Should create a user

// Should delete a user and his tokens

// Should edit a user field

// Should refresh a token

// Token should have a role assigned

// Should delete expired tokens
